name: Deployment
on:
  workflow_dispatch:     
  push:
    branches:
      - main
      - dev
env:
  MONGODB_DB_NAME: hemidemo
jobs:
  test:
    environment: demo
    env:
      MONGODB_PASSWORD: ${{ secrets.passwd }}
      MONGODB_USERNAME: ${{ secrets.user }}
      MONGODB_CLUSTER_ADDRESS: cluster0.awflcyp.mongodb.net
      PORT: 8080
    runs-on: ubuntu-latest
    container: node:20
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: |
          echo "Mongo DB user: "${{ env.MONGODB_USERNAME }}""
          echo "Mongo DB name: "${{ env.MONGODB_DB_NAME }}""
          echo "Mongo DB password: "${{ secrets.MONGODB_PASSWORD }}""
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "Mongo DB user: "${{ env.MONGODB_USERNAME }}""
          echo "Mongo DB name: "${{ env.MONGODB_DB_NAME }}""
